Run locally (from this folder):

```bash
corepack enable && corepack prepare yarn@4.3.1 --activate
yarn install
yarn start
```

By default, the docs site will auto‑load markdown from the parent `tech-notes/` directory.


